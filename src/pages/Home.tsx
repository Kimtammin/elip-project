import React from 'react';
import { Typography } from '@mui/material';

const Home = () => {
  return (
    <Typography variant="h4">Welcome to the E-learning Platform!</Typography>
  );
};

export default Home;
